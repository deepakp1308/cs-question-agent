# Optional dependencies — install only if you need the real adapter or renderer backend.
# The agent runs end-to-end without any of these using the mock adapter and the HTML fallback renderer.

# PDF rendering backend (requires system libs: cairo, pango, gdk-pixbuf, libffi)
# On macOS: `brew install cairo pango gdk-pixbuf libffi`
# On Ubuntu: `sudo apt install libcairo2 libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0 libffi-dev`
weasyprint>=62.0

# Real LLM adapters — each activates only if the matching env var is set.
openai>=1.40
anthropic>=0.34
google-generativeai>=0.7
cohere>=5.9

# Dev
pytest>=8
pytest-cov>=5
ruff>=0.5
